# Формат описан здесь: https://ansible-lint.readthedocs.io/en/latest/configuring.html
# По умолчанию исключаем пути к ролям и коллекциям, так как часть из них скачивается из Galaxy
# и они могут не соответствовать требованиям линтера.
# Для собственных ролей проверка линтером будет производиться отдельным пайплайном во время коммита в их репу.
exclude_paths:
  - ./lib
# skip_list:
#  - comments
